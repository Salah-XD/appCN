---
"@app-cn/cli": patch
---

Docs refresh: drop the "init is WIP" disclaimer in the CLI README, document
what `init` writes, and link to /docs/cli. No behavior change to the CLI
itself — README-only update to match the published wizard.
